I learned that my server currently starts listening before PostgreSQL confirms the connection because database connections are asynchronous. This works in development because the connection is established almost instantly, but a production application should verify database availability before accepting requests. A future improvement would be to await a successful database query before calling app.listen().